package internal

// Header constants
const (
	HeaderContentType = "Content-Type"
)

// Logging constants
const (
	LogFieldURL           = "url"
	LogFieldBusinessKey   = "business_key"
	LogFieldCamundaTaskID = "task_id"
)
